<h1>New Game</h1>
<div class="contaier">
  <ul>
    <% @letters.each do |letter| %>
    <li><%=letter.upcase%></li>
    <% end %>
  </ul>
</div>
<br>
<div class="container">
  <form action="/score" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :random_letters, @letters %>
    <input id="inputBar" type="text" name="user_input" placeholder="What's the longest english word?"><input id="button" type="submit" id="button" value="Ask">
  </form>
</div>
